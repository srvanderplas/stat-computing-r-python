venv_dir = "~/.virtualenvs"
venv_name = "book"

# renv::use_python(file.path(venv_dir, venv_name, "bin", "python"), type = "virtualenv", name = "book")
# reticulate::use_virtualenv(venv_name)
source("renv/activate.R")

if (file.exists("api_keys.R")) source("api_keys.R")
